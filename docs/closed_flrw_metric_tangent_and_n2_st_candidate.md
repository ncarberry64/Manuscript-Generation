# Closed-FLRW metric tangent and n=2 scalar/topographic candidate

## Hindsight correction

BHSM v6.22 showed that the historical critical-fold target delta R4=delta X
was not action-normalized.  X is an on-shell branch coordinate, not a local
scalar field, and different branches give different dR4/dX.

Cosmology therefore uses the direct metric Frechet derivative

DR_g[k] = nabla_mu nabla_nu k^(mu nu)
          - Box tr(k)
          - Ric_mu_nu k^(mu nu)

projected onto the physical closed-FLRW n=2 scalar sector.

## Canonical scalar/topographic candidate

The v5.7 lowest Robin zero-flux collar mode is constant, so lambda_rho=0.
The minimal harmonic-preserving extension candidate keeps unit normalized
trace amplitude, I2=1.

Thus

C_2,ST^cand = (1/L^2) [[5,-1],[-1,13]]

with determinant 64 and eigenvalues

(9-sqrt(17))/L^2,
(9+sqrt(17))/L^2.

This is strictly positive.

## Claim boundary

lambda_rho=0 and I2=1 are an explicit conditional extension candidate, not a
retained-action proof of the full nonhomogeneous trace/extension domain.

The remaining action calculation is A_g^(2), B_g2, and the physical scalar
metric domain on the closed-FLRW n=2 sector.
