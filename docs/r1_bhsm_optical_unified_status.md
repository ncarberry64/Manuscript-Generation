# Unified R1 / BHSM optical status

Integrated source checkpoints:

- exact R1 prediction-track tree through luminosity distance: $KernelCommit
- BHSM optical low-rank covariance line: $OpticalCommit

## Unified chain

    physical n=2 reduced propagator
      -> growth / Weyl response
      -> gauge-invariant closed-S3 luminosity-distance kernel

and

    BHSM local source statistics
      -> topographic / metric response
      -> optical redshift + convergence + shear state
      -> propagated path covariance
      -> the same gauge-invariant luminosity-distance observable layer.

The stochastic optical layer supplies accumulated path information; the R1
kernel performs the fixed-observed-redshift observable projection.

The SN scalar calibration has rank one on a two-dimensional physical n=2
state space. No second observational fit is authorized by this integration.
