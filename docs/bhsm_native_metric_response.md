# BHSM-native metric response

## Derived theorem

On the gauge-fixed physical tangent space, write the metric equation as

A_g h + B_g2 X_2 + J_g = 0.

If A_g is invertible on the physical metric domain, then

R_g2 = -A_g^{-1} B_g2

and

h_2 = h_src + R_g2 X_2.

The metric response therefore has no independent initial datum beyond the
already retained physical n=2 phase-space pair X_2=(q_2,Pi_2).

## Schur consistency

The same block elimination gives

K_2 = C_2 - B_2^dagger A_g^{-1} B_2,

so the metric-response theorem and the reduced topographic evolution theorem
are the same action reduction viewed from opposite blocks.

## BHSM source

The eta-metric mixed second variation already supplies an action-owned
metric/topographic coupling channel.  It is not by itself asserted to be the
complete cosmological propagator.

## Remaining numerical closure

Evaluate R_g2(z) and the corresponding matter-response map R_m2(z) on the
frozen R1 branch.  Once this is done, the observable amplitudes

A_O(z) = L_O,z [R_g2 + R_m2] X_2

are fixed by the reduced action rather than independently fitted.
