# Representation-invariant numerical bridge

The numerical prediction track does not require introducing a second scalar.

Let X2=(q2,Pi2) be the BHSM-native state and
Z2=(zeta2,Pi_zeta2) an effective closed-Horndeski coordinate system.

If X2=U Z2 with det(U)!=0, then

K_Z = U^dagger K_X U,
R_g,Z = R_g,X U,

and every linear observable is unchanged when its response is transformed
with the same U.

The exact R1 closed-Horndeski n=2 kernel can therefore be used as an effective
response representation, provided:

- the physical state count remains one scalar/two phase-space coordinates;
- U is nonsingular;
- normalization and kinetic sign match;
- the physical n=2 constraints are used;
- the SN calibration is not duplicated.

The earlier curved Newtonian-gauge DAE remains compatibility-audit material
only because its curvature-linked constraint-preservation defect is open.

This creates two closure tracks:

1. prediction: evaluate the R1 kernels in the exact reduced effective n=2
   representation;
2. provenance: evaluate the seven retained-action BHSM matrix elements.

The prediction track may close first, but remains labeled conditional until
the provenance track also closes.
