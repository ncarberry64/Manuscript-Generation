# Physical n=2 metric KKT projection

## Domain architecture

Later retained-action fixed-h work supplies the exact sequence:

1. vary before gauge fixing;
2. solve the momentum equation;
3. choose E=0;
4. impose psi(1)=0 on the induced-metric trace;
5. retain eta_tr=P_psi(1);
6. quotient endpoint-preserving radial diffeomorphisms.

The matcher pairing cancels the endpoint canonical-momentum Green current, so
the extended KKT operator and adjoint domains coincide in that ensemble.

Cosmology adopts this as an action-derived domain architecture candidate, but
does not import the fold/background-specific radial coefficients.

## Finite remaining metric data

A_g^(2) = [[a_AA,a_Apsi],[a_Apsi,a_psipsi]]

B_g2 = [[b_Aq,b_APi],[b_psiq,b_psiPi]]

R_g2 = -(A_g^(2))^-1 B_g2.

The remaining metric calculation is therefore seven action matrix elements
plus confirmation of the physical domain.

## Mixed source provenance

The metric/topographic mixed second variation is already action-owned. Its
projection onto the physical n=2 mode and the A/psi metric basis defines the
four B_g2 entries. They are not new phenomenological couplings.
