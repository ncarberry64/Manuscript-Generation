
# Claim status Ã¢â‚¬â€ BHSM/topographic cosmology manuscript

## Established source inputs
- Closed hyperspherical parent `S^3(R_H)`.
- Cubic-Galileon / kinetic-gravity-braiding cosmological action used in the later Topographic Dark Energy formulation.
- Standardized scalar spectrum: `-Delta Y_n = n(n+2)Y_n/R_H^2`.
- Carried-forward low-z Pantheon+SH0ES tomographic calibration: `A_mu = -0.0412 Ã‚Â± 0.009` in `0.01 < z < 0.03`, axis `(RA,Dec)=(211.48Ã‚Â°, -12.81Ã‚Â°)`. The current repository does not independently rerun that tomographic likelihood.
- Existing prospective curvature forecast: `Omega_k = -0.018 Ã‚Â± 0.004`; retained as a frozen model forecast, not a current empirical best fit.

## Exact bridge mathematics
- Physical Schur/Feshbach reduction: `K_red = A - C D^{-1} CÃ¢â‚¬Â `.
- For the declared illustrative block, the long-wave expansion generates a negative `p` correction and positive `p^2` stiffness when the complement coefficient has the stated sign.
- Exact `n=1` scalar observer-shell decomposition on `S^3`: generic scalar mode = sky monopole + sky dipole.
- For `1/8 < ell^2 < 1/3`, the reduced spatial filter softens `n=1` and stiffens every `n>=2`.

## Referee-critical caveats
- In a common closed-FLRW convention, our `n=1` scalar level maps to literature `N=2`, where standard gauge-invariant scalar-metric constructions contain exceptional/singular factors such as `1/(N^2-4K)` for `K=+1`.
- Therefore the exact scalar sky dipole is **not yet** a proof of a physical metric, velocity, or luminosity-distance dipole. The exceptional scalar-metric sector must be derived explicitly.
- `D_kin = alpha_K + 3 alpha_B^2/2 > 0` is retained as an inherited cubic-Galileon kinetic diagnostic. Bellini-Sawicki's standard perturbation formulation assumes spatial flatness, so this quantity alone is not a complete closed-FLRW stability proof.
- The `n=2` compact-space response bound is not directly a sky quadrupole bound. Observer-shell projection and source weights are required.

## Exploratory bridge assumptions
- `L_c(a)=a R_H ell`.
- Illustrative mixed block `C(p)=g sqrt(p)`; not yet microscopically derived from BHSM.
- Minimal source-response form `T_n=S_n/[p_n C_n]`.
- Reference Branch R1: `Omega_m0=0.31`, `Omega_r0=9e-5`, `Omega_k0=-0.018`, `lambda=1`, `q=3`.
- Any no-slip closure `gamma=1` is R1-specific unless separately derived.
- R1 growth numbers use the high-n/background-only approximation for the direct fifth-force term.

## New derived reference-branch quantities
- R1 shooting solution: `V0/(M_Pl^2 H0^2) Ã¢â€°Ë† 2.39861`.
- R1 growth ratios are generated by `code/growth.py`.
- The numerical manifest remains frozen at the pre-audit SHA-256; this referee pass changes claims/citations, not the preregistered numerical targets.

## Not claimed
- Full BHSM microscopic-to-cosmological compactification theorem.
- Completed gauge-invariant scalar-metric derivation of the exceptional lowest closed-FLRW mode.
- Complete perturbative stability proof for closed FLRW plus the reduced topographic kernel.
- Unique derivation of `q`, `lambda`, `ell`, or the source normalization.
- Unique microscopic origin of the observed supernova dipole axis.
- Proof that the entire Hubble tension is explained by the carried-forward low-z distance dipole.


## Round-2 correction: first physical compact-space scalar mode
- `n=1` maps to the exceptional literature `N=2` sector and is not retained as the ordinary propagating scalar carrier.
- The carrier is promoted to manuscript `n=2` / literature `N=3`.
- The pure `A_{4i}` degree-two subspace is an exact sky dipole with radial factor `sin(2 psi)`.
- The corrected filter interval is `1/15 < ell^2 < 1/8`.
- Higher physical harmonics `n>=3` are stiffened.
- The frozen R1 numerical manifest remains unchanged.
## Round-3 exact closed-Horndeski kernel
- The non-flat Horndeski scalar action is specialized explicitly to the manuscript cubic-Galileon/KGB model.
- For constant G4 and G5=0, the explicit curvature pieces Theta_K and Sigma_K vanish.
- The physical manuscript n=2 lapse/shift constraints are solved algebraically.
- The exact gravity-scalar kinetic coefficient is
  `G_S,2 = M_Pl^2 * 5 D_kin / [D_kin + 10(1-alpha_B/2)^2]`.
- Therefore `D_kin>0` implies `G_S,2>0` provided `Theta != 0`.
- This is not yet a full matter-era stability or luminosity-distance transfer proof because matter/radiation perturbations and the reduced topographic operator remain to be included.
