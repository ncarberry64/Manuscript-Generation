# Scientific referee audit — Round 2
## Exceptional-mode resolution and first physical harmonic

Date: 2026-09-18

Manuscript `n=1` maps to literature `N=2`. For an `n=1` scalar harmonic on unit `S^3`, `D_iD_j Q=-gamma_ij Q`, so the scalar-derived trace-free metric harmonic vanishes. Under the regular generally covariant one-scalar constraint structure, that sector does not carry the ordinary propagating scalar degree of freedom. This is consistent with modern closed-universe analyses in which physical scalar modes begin at alternative-convention `N>=3`.

The topographic carrier is therefore promoted to manuscript `n*=2`, i.e. literature `N*=3`.

Degree-two `S^3` scalar harmonics are traceless quadratic forms `A_AB X^A X^B`. Their pure cross-term subspace gives the exact observer-sky dipole

`T_2,dip = A sin(2 psi) (n_hat . p_hat)`.

To soften `n=2` while stiffening all physical `n>=3`, the corrected filter interval is

`1/15 < ell^2 < 1/8`.

The next physical source-normalized response is

`|(T_3/S_3)/(T_2/S_2)| = 8 C_2/(15 C_3)`.

The original R1 numerical manifest is unchanged because its background and high-n growth forecasts do not depend on the identity of the selected curvature-scale harmonic.
