V1 sightline analysis plan, fixed before evaluating any new topographic likelihood.

Frozen scientific state: retain the previous R1 cosmology, axis (211.48,-12.81) degrees, amplitude -0.04321623435997208 mag, zref=.02, zc=.03, pointwise local redshift transfer, and conditional 1:5 model. Primary comparison is null versus frozen local topographic prediction. A separate one-scalar amplitude estimate is explicitly fitted and never replaces the predictive amplitude. Preserve Pantheon training [.01,.03), held-out [.03,1), DES survey-10 [.10,1.20) with the previous 152 shared-SN exclusions. No residual-dependent sample changes.

Level 1: obtain all public Pantheon SNANA photometry, released passband/primary-standard kcor FITS, nominal and nine released SALT2 realizations; obtain DES raw FITS photometry, nominal and ten SALT3 Dovekie realizations. Reconstruct individual epoch/band data with a local SALT likelihood around the published source parameters, separating flux normalization, color, shape and peak phase, and inspect remaining band/phase contrasts. Use the released [-15,45] rest-day fit window for diagnostics, positive finite measurement uncertainties, at least five measurements, and model wavelength coverage. These are fixed diagnostic identifiability conditions, not new cuts on the primary HD sample. Record every unavailable, ambiguous or failed reconstruction. Preserve epochs, raw-file/survey identity, fit parameters and local covariance. Distinguish this reconstruction from an exact reimplementation of SNANA selection, intrinsic scatter and BBC bias calibration. Validate band-system normalization before use; record any nominal calibration mapping that cannot be reproduced exactly.

For each identifiable source perform leave-one-band-out and leave-one-rest-phase-block-out (-15..0,0..15,15..45 days) reconstructions and all released spectral/calibration variants. Record common-amplitude shifts and shape/color/phase responses. Do not alter or reject SNe based on those results. Absolute distance/common-mode residual for the proper released HD covariance remains the published standardized MU minus frozen R1 distance; epoch reconstruction supplies a separate common-amplitude departure and orthogonal non-common diagnostics. Do not replace the released MU vector with raw-refitted amplitudes while retaining its old covariance. A new fully propagated distance covariance is unavailable unless the full calibration/BBC response is reproduced.

Level 2: use Lilow et al. 2MRS-NeuralNet galaxy-only density/velocity fields, trained on simulation mocks rather than fitted to these SN residuals. Fix their supplied means/errors and valid radius 200 h^-1 Mpc; do not fit a velocity normalization/direction. Evaluate in published Galactic Cartesian coordinates with R1 radial distances; note the reconstruction's own fiducial cosmology. Preserve supplied density and velocity marginal errors; do not invent spatial error covariance. Use published SN dust and host/environment measurements with explicit missingness and provenance. Beyond the external map, mark endpoint PV/environment unavailable, never assume the remaining path is empty or velocity-free. No independent PV covariance is constructed by covariance subtraction.

Construct local foreground shell densities on the pre-existing shell boundaries [0,.01,.03,.05,.10,.15,.30,.50,1.0], clipping map integrations at 200 h^-1 Mpc and retaining fractional coverage. Build a sparse path-length matrix W_ik on 10 h^-1 Mpc Galactic Cartesian cells crossed by each sightline, labeled by these radial shells. Integrate with 2 h^-1 Mpc midpoint steps; cell size and integration are fixed without residual inspection. Foreground cells beyond the map have geometry but no measured density; no full-path environmental claim follows. Include a geometry-weighted local lensing proxy using the original closed-space Green kernel, labelled a proxy, not measured full lensing convergence.

Construct nuisance columns before opening new template comparisons: released shape/color, Milky Way E(B-V), host log mass, independent shell-density averages, external fixed PV correction difference where covered, and available orthogonal LC color/shape/phase diagnostics plus missingness indicators. Do not use the raw common-amplitude departure as a nuisance column. Standardize on training rows only. Use full-covariance GLS/SVD, deterministic numerical rank threshold 1e-10, no residual-selected features. Nuisance coefficients are learned in training folds; held-out rows only profile original bin monopoles/slopes. Report a conservative conditional release-covariance test, not a fully marginalized new reduction when external field/error cross-covariances are unavailable. For external-PV-covered rows, separately report the fixed-map redshift-mean replacement as a sensitivity diagnostic using unchanged covariance; this is not a new fully recalibrated likelihood.

Validation folds: four RA quadrants, and leave-one-IDSURVEY-out Pantheon (exclude every training observation of an SN appearing in test). Baseline no-recalibration Pantheon training-to-held-out and independent-SN DES tests retain the old frozen amplitude; external/environment nuisance relations use only training rows. For the main source/environment analysis cross-fit nuisance relations on all original Pantheon [.01,1), score excluded sectors/surveys, and transfer the all-Pantheon nuisance relation to DES. A survey seen only in test cannot inherit a fitted survey offset; original test-bin monopoles remain nuisance. Distinguish overlap-clean DES from genuinely independent calibration.

Matched geometry tests use deterministic non-reused SN pairs within survey where possible: same redshift |delta zHD|<=.005 and separation>=60 degrees; same direction separation<=5 degrees and |delta zHD|>=.03; shared foreground normalized W-overlap>=.5 and |delta zHD|>=.03. Remove duplicate-SN pairs. Compare low/high foreground density using the median independent path-density predictor fixed from Pantheon geometry, not residuals. Carry covariance through pair-difference matrices and retain survey/subset labels. No significance from counting correlated pairs as independent.

Calibrate the fixed-template score with 2,000 Gaussian full-covariance null mocks (seed 20260920) and 2,000 conditional design permutations. Permute template labels within fixed survey, redshift-bin and RA-quadrant strata, preserving positions/redshifts and selection in the data; move duplicate-SN blocks together. Because covariance and irregular sampling break exchangeability, report empirical permutation tails as conditional diagnostics and verify their null behavior against Gaussian mocks; use the Gaussian-calibrated tail as the inferential result. No unrestricted rotation/axis optimization. Preserve simulated/permuted test statistics and seed.

For residual correlations compare the fixed coherent template score with local cell-sharing covariance score tests from W W^T after nuisance projection. Use shell-separated kernels and mock calibration including the maximum over shells (multiplicity), without fitting arbitrary cell amplitudes to held-out SNe. Report W rank and common volumes; distinguish cell-localized residual correlation from a coherent mean pattern and from survey calibration correlation. Full multi-observable or full-path environmental attribution is not claimed if required measurements/cross-covariances are absent.

Deliver all tables, matrices, fits/failures, provenance, scripts, checks, figures and a final Markdown report in this directory. Success includes a transparent negative/inconclusive result; no data-driven rescue changes are allowed.
